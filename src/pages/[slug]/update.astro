---
export function getStaticPaths() {
	return []
}
import Layout from '../../layouts/Layout.astro'
const {slug} = Astro.params
---

<Layout title="@{slug}:radio4000.com">
	<h1>{slug}</h1>
	<r4-channel slug="{slug}"/>
</Layout>
